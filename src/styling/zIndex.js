/* All z-index values must be added here */
const zIndex = {
  toast: 200,
  modal: 100,
  headerBar: 10,
  navBar: 12,
  playerOverlay: 14,
  playerOverlayDesktop: 14,
  miniPlayer: 1000,
  sideMenu: 15,
  ad: 20,
  disableOverlay: 5,
  socialPopup: 6,
};

export default zIndex;
